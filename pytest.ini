[pytest]
python_files = test_*.py
python_classes = Test*
python_functions = test_*
testpaths = tests
markers =
    unit: unit tests (no hardware)
    integration: integration tests (require imports)
addopts = -v --tb=short -ra
