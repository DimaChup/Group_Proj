\section{Evaluation}\label{sec:evaluation}

The central question is: \emph{how close is the system to reliably finding a casualty in the field, and what evidence supports that assessment?} Section~\ref{sec:requirements} established that 8 of 12 requirements are verified at bench or higher fidelity, 3 in simulation only, and 1 partially. The plus/delta review below draws on four evidence sources: SITL simulation (Tier~1, 100+ hours), Docker environment tests (Tier~2), bench tests on the assembled Pi~5 hardware (Tier~3, one field day), and DJI flight video replayed through the detection pipeline (Tier~3 proxy). No outdoor flight took place (Tiers~4--5 outstanding). D7 addresses team-working aspects individually.

% ─────────────────────────────────────────────────────────────────────
\subsection{Plus/Delta Summary}\label{sec:plus-delta}

Table~\ref{tab:plus-delta} summarises the principal strengths and areas for improvement. Each item includes the specific evidence that supports the assessment.

\begin{table}[ht!]
\centering\compactTable
\caption{Plus/delta review of system technical performance.}\label{tab:plus-delta}
\begin{tabularx}{\linewidth}{@{}c p{5.2cm} X@{}}
\toprule
& \textbf{Item} & \textbf{Evidence / Detail} \\
\midrule
\multicolumn{3}{@{}l}{\textbf{Plus --- strengths}} \\
\addlinespace
\color{green!50!black}\textbf{P1} & Dual-backend CV architecture & Identical application code runs Ultralytics on the laptop and TFLite on the Pi; model files are drop-in interchangeable. The model was swapped three times (640, 1280, 1088 datasets) with zero changes to application code (\secref{sec:cv}). \\
\addlinespace
\color{green!50!black}\textbf{P2} & Configuration auto-detection & Serial-port probing selects real Cube or SITL with zero code changes. Verified by running the same \texttt{main.py} on Windows laptop (SITL), WSL (SITL), and Pi~5 (real Cube) without modification (\secref{sec:sw-arch}). \\
\addlinespace
\color{green!50!black}\textbf{P3} & Progressive testing caught 12 defects at low-cost tiers & The five-tier framework discovered 12 integration faults before any flight attempt (Table~\ref{tab:bug-cost}): BGR colour inversion, FOV miscalibration ($f{=}7.0{\to}5.46$\,mm), geofence sign errors, TFLite bounding box coordinate mismatch, GPS estimate double-scaling, pyserial breakage, and 6 others---all resolved in 5--120\,min at the bench tier. Seven of these are documented as evidence-based corrections in Section~\ref{sec:decisions-changed}. Total debugging time: ${\sim}$\SI{6}{\hour} 10\,min across 12 defects; estimated cost if discovered in flight: 2 crashed airframes (\pounds2\,000+) and regulatory violation (LL-08). See \secref{sec:bug-discovery} and Appendix~K for the full defect register. \\
\addlinespace
\color{green!50!black}\textbf{P4} & On-device inference performance & \SI{4.8}{FPS} (\SI{206.5}{\milli\second}/frame) on Pi~5 CPU, measured over 50 runs with \SI{0.995}{} mAP$_{50}$ on the retrained model; 50/50 detections at 0.966 mean confidence in the bench benchmark (\secref{sec:cv}). \\
\addlinespace
\color{green!50!black}\textbf{P5} & Modular vision interface & \texttt{vision.py} is the sole file that touches CV/AI; all other modules call \texttt{detect\_in\_image(frame)} $\to$ \texttt{(found, x, y, conf)}. Adding lens undistortion required modifying one file and added only \SI{1.5}{\milli\second} per frame---\SI{0.7}{\percent} of inference time (\secref{sec:cv}). \\
\addlinespace
\color{green!50!black}\textbf{P6} & Web-based ground station & Browser dashboard with MJPEG stream, GPS grid, and command buttons operates headlessly over SSH at \texttt{http://PI\_IP:8090}. Tested from PuTTY terminal and phone browser during bench day (\secref{sec:groundstation}). \\
\addlinespace
\color{green!50!black}\textbf{P7} & 74 test scripts across 8 categories & 74 scripts totalling ${\sim}$5\,500 LOC across hardware checks (8), flight progressions (14), diagnostics (5), calibration (7), field experiments (6), development tools (21), unit tests (6 files, 127 tests), and automated integration tests (7 files, 33 tests)---each single-purpose with CSV output where applicable. See Appendix~N for the full script inventory and Appendix~K (\secref{sec:test-org-detail}) for the categorisation rationale. \\
\addlinespace
\color{green!50!black}\textbf{P8} & Simulation-first development & Full state machine, geofence enforcement, and detection pipeline were validated end-to-end in SITL before hardware connection; the same \texttt{main.py} runs on both platforms. Dry-run mode validates waypoints in under 1\,s without any hardware (\secref{sec:simulation}). \\
\addlinespace
\color{green!50!black}\textbf{P9} & Reproducible quantitative evidence & All performance figures (Figures~\ref{fig:det_sensitivity}--\ref{fig:perf_analysis}) are derived from measured data (calibration, inference benchmarks, flight video analysis) via archived generator scripts (\texttt{figs/gen\_*.py}), ensuring traceability and enabling re-evaluation with updated data. \\
\addlinespace
\color{green!50!black}\textbf{P10} & Centering mitigates five error sources & The CENTERING hover procedure simultaneously eliminates roll/pitch error (\SI{6.2}{\metre}$\to$$<$\SI{0.6}{\metre}), motion blur, and GPS timing lag while enabling multi-frame GPS averaging ($\sqrt{N}$ improvement) and temporal false-positive filtering---solving sensor limitations through mission design rather than software complexity (Appendix~\ref{sec:centering-mitigation}). \\
\midrule
\multicolumn{3}{@{}l}{\textbf{Delta --- areas for improvement}} \\
\addlinespace
\color{red!70!black}\textbf{D1} & No outdoor flight completed & Sustained winds (\SI{12}{\metre\per\second}, gusts to \SI{18}{\metre\per\second}) on the single scheduled flight day exceeded the EDU-450's \SI{10}{\metre\per\second} operational limit. Result: 0 of 5 tiers exercised outdoors; requirements R05 (search), R06 (PLB), R07 (landing accuracy) are verified in SITL only. Of the 12 requirements, 8 are fully verified (bench + simulation), 3 partially (simulation only), and 1 has a partial gap (R06 PLB not field-tested). \emph{Next step:} rebook flight slot; execute Tiers~3--5 of the progressive framework with the identical codebase. \\
\addlinespace
\color{red!70!black}\textbf{D2} & Inference rate limits high-speed search & At \SI{4.8}{FPS} (\SI{206.5}{\milli\second}/frame, 50-run benchmark) and \SI{8}{\metre\per\second} search speed, inter-frame advance is \SI{1.7}{\metre}---\SI{5.3}{\percent} of the \SI{32}{\metre} ground footprint at \SI{35}{\metre} altitude, so overlap is adequate. However, NCNN benchmarks on Pi show \SI{72}{\milli\second}/frame (\SI{9}{FPS}), a $4.5\times$ improvement already available but not deployed due to limited Pi testing time (Appendix~H2, \secref{app:cv:benchmarks}). \emph{Next step:} deploy NCNN backend, validate detection accuracy parity, and thread the capture/inference pipeline for 20--30\% additional throughput. \\
\addlinespace
\color{red!70!black}\textbf{D3} & GPS timing lag uncompensated & The Here~3+ receiver exhibits \SIrange{100}{200}{\milli\second} latency, producing \SI{0.8}{}--\SI{1.6}{\metre} systematic error at \SI{8}{\metre\per\second}. DJI video analysis measured CEP$_{50}{=}\SI{2.3}{\metre}$, worst case \SI{16.5}{\metre} (Appendix~J, Figures~\ref{fig:gps_bullseye}--\ref{fig:gps_error_direction}). Inverse-variance averaging partially mitigates this, but first-order lag compensation ($\Delta\mathbf{p} = \mathbf{v} \cdot \Delta t_{\text{lag}}$) is not implemented. \emph{Next step:} implement lag compensation in the heading direction, tuneable from config. \\
\addlinespace
\color{red!70!black}\textbf{D4} & No precision--recall curve for confidence threshold & The detection threshold of 0.2 was chosen empirically from video replay; no systematic sweep across thresholds was performed to characterise the precision--recall trade-off. \emph{Next step:} run threshold sweep on DJI video with ground-truth annotations to produce a PR curve and select the operating point formally. \\
\addlinespace
\color{red!70!black}\textbf{D5} & Train/validation overlap in model training & The training set (300 synthetic + 16 real + 50 negatives = 366 images) and validation split share the same generation pipeline, inflating the reported mAP$_{50}{=}0.995$. The 16 real images (\SI{4.4}{\percent} of the dataset) provide some cross-domain signal but are too few for a statistically meaningful held-out test. The mAP figure should be treated as an \emph{upper bound}. \emph{Next step:} collect 50+ real labelled frames from a separate flight and evaluate on a held-out test split with zero synthetic data (see Appendix~C, \secref{app:cv:training}). \\
\addlinespace
\color{red!70!black}\textbf{D6} & Single-class detector cannot distinguish target types & The YOLOv8n model detects a single ``dummy'' class; it cannot visually distinguish the rescue mannequin from a person, bag, or similar-sized object. Classification relies on operator confirmation at the VERIFY state. \emph{Next step:} retrain with multi-class labels (person, dummy, debris) or add a lightweight classifier head downstream of detection. \\
\addlinespace
\color{red!70!black}\textbf{D7} & PLB Focus Area not field-tested (R06) & Focus area replanning is implemented and verified in SITL, but has not been triggered during a real flight with live GPS coordinates. \emph{Next step:} inject a PLB message mid-flight via the ground station HTTP endpoint during Tier~4 testing. \\
\addlinespace
\color{red!70!black}\textbf{D8} & Payload release not software-integrated & The Tarot release mechanism is mounted but its servo actuation is not wired into the state machine; first-aid deployment (R07) would require manual RC override. \emph{Next step:} add a \texttt{DEPLOY} state between APPROACH and LANDING with MAV\_CMD\_DO\_SET\_SERVO trigger. \\
\addlinespace
\color{red!70!black}\textbf{D9} & Float32 model on constrained hardware & The deployed TFLite model uses float32 weights (\SI{11.7}{MB}). FP16 dispatch on the Cortex-A76 cores is estimated to halve inference time to ${\sim}\SI{100}{\milli\second}$ (\SI{10}{FPS}); INT8 quantisation would further reduce latency and model size to ${\sim}\SI{3}{MB}$, at an accuracy cost that has not yet been characterised. Neither optimisation was deployed due to time constraints (Appendix~H2, \secref{app:cv:latency}). \emph{Next step:} export FP16 model, benchmark on Pi, and measure accuracy regression vs.\ float32 baseline. \\
\bottomrule
\end{tabularx}
\end{table}

% ─────────────────────────────────────────────────────────────────────
\subsection{Discussion of Key Findings}\label{sec:eval-discussion}

\begin{figure}[ht]
\centering
\begin{subfigure}[t]{0.48\textwidth}
  \centering
  \includegraphics[width=\linewidth]{conf_vs_alt}
  \caption{Detection confidence vs.\ altitude. Confidence remains above 0.9 up to \SI{40}{\metre}, dropping at higher altitudes as the target subtends fewer pixels.}
  \label{fig:conf_vs_alt}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.48\textwidth}
  \centering
  \includegraphics[width=\linewidth]{det_vs_speed}
  \caption{Detection rate vs.\ flight speed. At the nominal search speed of \SI{8}{\metre\per\second}, detection rate exceeds 95\%.}
  \label{fig:det_vs_speed}
\end{subfigure}
\caption{Detection performance sensitivity analysis from DJI flight video replay.}
\label{fig:det_sensitivity}
\end{figure}

\begin{figure}[ht]
\centering
\begin{subfigure}[t]{0.48\textwidth}
  \centering
  \includegraphics[width=\linewidth]{latency_breakdown}
  \caption{Per-frame latency breakdown: capture, undistortion, resize, inference, and postprocessing.}
  \label{fig:latency_breakdown}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.48\textwidth}
  \centering
  \includegraphics[width=\linewidth]{detection_heatmap}
  \caption{Spatial detection heatmap showing detection density across the survey area during simulated search.}
  \label{fig:detection_heatmap}
\end{subfigure}
\caption{System performance: inference latency budget and spatial detection distribution.}
\label{fig:perf_analysis}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{detection_montage}
\caption{Detection montage from DJI flight video replay at varying altitudes and viewing angles. Green bounding boxes show YOLOv8n detections with confidence scores. The target (rescue mannequin) is reliably detected from \SI{15}{\metre} to \SI{40}{\metre} altitude under real outdoor lighting conditions.}
\label{fig:detection_montage}
\end{figure}

\textbf{Architecture resilience (P1, P2, P5, P8).}
The single-interface vision module (\texttt{detect\_in\_image}) enabled three model retrains (640, 1280, 1088\,px datasets) with zero changes to the orchestrator or ground station. Adding lens undistortion---not anticipated during initial design---required modifying one file and added only \SI{1.5}{\milli\second} per frame.

\textbf{Detection performance (P4, D2, D4, D5, D9).}
The retrained YOLOv8n achieved mAP$_{50}$ = 0.995 (validation set) and 50/50 bench detection at 0.966 mean confidence (Figures~\ref{fig:conf_vs_alt} and~\ref{fig:detection_montage}). At \SI{35}{\metre}, the target subtends 36 model-input pixels---a comfortable margin above the 20\,px floor. However, the 0.995 mAP$_{50}$ likely overestimates real-world accuracy due to train/validation overlap (only 16 of 366 images are from real footage). The confidence threshold (0.2) was set by visual inspection rather than a precision--recall curve sweep; characterising this trade-off is a priority for future work. FP16 deployment could double throughput to ${\sim}$\SI{10}{FPS} (Figure~\ref{fig:latency_breakdown}).

\textbf{GPS estimation accuracy (D3).}
The pipeline projects each detection's pixel offset through the calibrated camera model and drone position to produce a GPS estimate. DJI flight video analysis with known ground-truth positions yielded CEP$_{50}$ = \SI{2.3}{\metre} and a worst-case error of \SI{16.5}{\metre} (Figures~\ref{fig:gps_bullseye} and~\ref{fig:gps_error_direction}). The worst case stems from GPS timing lag compounding with heading-aligned motion. Inverse-variance averaging and Kalman filtering partially mitigate this bias, but first-order lag compensation ($\Delta\mathbf{p} = \mathbf{v} \cdot \Delta t_{\text{lag}}$) remains unimplemented.

\textbf{Testing effectiveness (P3, P7).}
The progressive framework caught 12 defects at Tiers~1--3, each resolved in minutes to hours at zero hardware risk: 5 at Tier~1 (simulation, 1.5\,hr total), 6 at Tier~2 (Pi bench, 4.7\,hr), and 1 at Tier~3 proxy. Seven would have caused mission-critical failures in flight: geofence sign inversion (drone pushed \emph{toward} SSSI), BGR colour inversion (detection failure), TFLite bounding box coordinate mismatch (GPS estimates off by $>$\SI{100}{\metre}), repulsive force reversal, barometric drift loop, duplicate target queuing, and missing timeout guard---yet all were trivially reproducible in simulation or on the bench. Appendix~\ref{app:bug-cost} provides the full defect register with resolution times and assessed flight-day impact.

\textbf{Weather cancellation as information-maximisation, not failure (D1).}
The outdoor flight was cancelled due to sustained winds of \SI{12}{\metre\per\second} (gusts to \SI{18}{\metre\per\second})---above the EDU-450's \SI{10}{\metre\per\second} limit. This is the single largest gap in the project, eliminating Tier~4/5 validation. However, the team treated the bench day as an opportunity to maximise information yield, executing four activities that would not have occurred had the flight proceeded:

\begin{enumerate}[nosep]
    \item \textbf{FOV calibration} ($f{=}7.0 \to 5.46$\,mm, tape measure at 1\,m) --- corrected a \SI{28}{\percent} GPS estimation bias that would have produced landing errors exceeding the \SI{10}{\metre} R07 radius.
    \item \textbf{Lens distortion mapping} (RMS = 0.399, 14$\times$9 checkerboard) --- confirmed that the IMX296 global shutter has negligible barrel distortion and undistortion is optional.
    \item \textbf{Inference benchmarking} (50 runs $\times$ 3 models) --- established the \SI{206.5}{\milli\second} baseline and validated 50/50 detection at 0.966 mean confidence.
    \item \textbf{DJI video analysis pipeline} (built post-cancellation) --- yielded the confidence-vs-altitude curve (Figure~\ref{fig:conf_vs_alt}), detection-vs-speed analysis (Figure~\ref{fig:det_vs_speed}), and GPS accuracy characterisation (CEP$_{50}{=}\SI{2.3}{\metre}$, Figure~\ref{fig:gps_bullseye}).
\end{enumerate}

\noindent These four data products form the quantitative backbone of this evaluation. The project lacks \emph{system-level} flight validation but possesses \emph{subsystem-level} quantitative data exceeding what a single flight sortie would typically yield (Appendix~\ref{sec:field-day}).

\textbf{Incomplete flight validation (D1, D6, D7, D8).}
The integrated system has never executed a search pattern over real terrain. Detection range versus altitude in natural lighting, false-positive rate against real backgrounds, wind-induced motion blur, and GPS accuracy under dynamic manoeuvres all remain unvalidated---only DJI video proxy data are available. Bench results suggest outdoor operation will require parameter tuning rather than architectural changes.

% ─────────────────────────────────────────────────────────────────────
\subsection{Comparison with Published SAR Systems}\label{sec:eval-comparison}

Table~\ref{tab:sar-comparison} positions this project's quantitative results against published SAR drone systems. Direct comparison is imperfect---differing hardware, environments, and target types---but the table provides context that internal metrics alone cannot.

\begin{table}[ht]
\centering\small
\caption{Performance comparison with published SAR drone systems. Our results are from bench/video proxy (no flight); published results are from outdoor flights.}\label{tab:sar-comparison}
\begin{tabular}{@{}p{2.6cm}p{2.0cm}p{1.8cm}p{1.5cm}p{1.6cm}p{2.4cm}@{}}
\toprule
\textbf{System} & \textbf{Platform} & \textbf{Detector} & \textbf{FPS} & \textbf{mAP$_{50}$} & \textbf{GPS Est.\ Acc.} \\
\midrule
This project & Pi~5 CPU & YOLOv8n TFLite & 4.8 & 0.995$^*$ & CEP$_{50}$=2.3\,m$^\dagger$ \\
AUSPEX~\cite{auspex2025} & Jetson Orin & YOLOv5s & 30 & 0.89 & $<$5\,m (RTK) \\
Sambolek~\cite{sambolek2021} & Jetson TX2 & YOLOv3-tiny & 22 & 0.81 & Not reported \\
Drones+YOLO~\cite{realtimesar2025} & Jetson Nano & YOLOv8s & 12 & 0.91 & Not reported \\
SearchWing~\cite{searchwing2024} & Companion PC & Custom CNN & 5 & 0.78 & $<$10\,m \\
\bottomrule
\multicolumn{6}{@{}l}{\footnotesize $^*$Upper bound; train/val overlap inflates this figure. Realistic estimate ${\sim}$0.85--0.90 based on 87\% video detection rate.} \\
\multicolumn{6}{@{}l}{\footnotesize $^\dagger$From DJI video replay, not live flight.} \\
\end{tabular}
\end{table}

Figure~\ref{fig:radar_comparison} presents a radar chart comparing the system's capabilities against published SAR platforms across six key dimensions.

\begin{figure}[H]
\centering
\includegraphics[width=0.7\linewidth]{radar_comparison}
\caption{Multi-dimensional comparison of this project against published SAR drone systems (Table~\ref{tab:sar-comparison}). Axes: inference speed, detection accuracy, GPS estimation accuracy, cost-effectiveness, autonomy level, and test coverage. This project excels in cost-effectiveness and test coverage but lags in inference speed due to CPU-only deployment.}
\label{fig:radar_comparison}
\end{figure}

Three observations emerge. First, the mAP$_{50}$=0.995 is almost certainly inflated; the 87\% detection rate on DJI video suggests a realistic mAP$_{50}$ of 0.85--0.90, competitive with published systems trained on larger, more diverse datasets. Second, the 4.8\,FPS inference rate sits at the lower end, since most published systems use GPU-equipped Jetson boards costing 3--5$\times$ more. The available NCNN backend (\SI{72}{\milli\second}, 9\,FPS) would close this gap without hardware changes. Third, no published system in Table~\ref{tab:sar-comparison} reports a formal GPS estimation accuracy metric from vision-based localisation; the CEP$_{50}$=\SI{2.3}{\metre} figure, while from proxy data, is a quantitative contribution most comparable projects lack.

% ─────────────────────────────────────────────────────────────────────
\subsection{Evaluation of Design Choices}\label{sec:eval-alternatives}

A credible evaluation must question not only \emph{how well} the system performed, but whether the \emph{right system} was built in the first place.

\textbf{Pi~5 CPU vs.\ Jetson GPU.}
The Pi~5 was chosen for cost (\pounds75 vs.\ \pounds400+ for a Jetson Orin Nano), ecosystem familiarity, and picamera2 support. The trade-off is inference speed: 4.8\,FPS on CPU versus 12--30\,FPS on Jetson GPU (Table~\ref{tab:sar-comparison}). In hindsight, CPU-only inference is adequate at 8\,m/s (1.7\,m inter-frame advance vs.\ 32\,m footprint), but a Jetson would have provided headroom for multi-class detection, higher resolution, or faster search. The NCNN backend partially recovers this gap at zero additional cost.

\textbf{Python + MAVLink vs.\ ROS\,2.}
Direct MAVLink avoided the complexity of a ROS\,2 workspace, launch files, and message type compilation---the pragmatic choice for a team where only one member had significant Python experience. However, ROS\,2 would have provided structured IPC, standardised logging (rosbag), and hardware abstraction that would simplify multi-sensor integration (e.g.\ thermal camera). The current ad-hoc CSV telemetry recording is a direct consequence of this decision.

\textbf{Lawnmower vs.\ adaptive search.}
The boustrophedon (lawnmower) pattern is the standard baseline for area search and guarantees 100\% coverage of the polygon. Adaptive strategies---spiral search, expanding square, or reinforcement-learning-based prioritisation~\cite{deeprlsar2025}---could reduce mean time-to-detection by biasing search toward high-probability areas, but require a prior probability map that was unavailable for this project's test site. Given the small survey area (${\sim}$0.04\,km$^2$) and the single-pass coverage requirement, the lawnmower pattern's simplicity and completeness guarantee were the correct choice. For larger areas, adaptive planning would become necessary.

\textbf{Single-class vs.\ multi-class detector.}
The single-class ``dummy'' detector (D6) defers classification to the human operator at the VERIFY state. This aligns with the Sheridan Level~3--4 framework (Section~\ref{sec:autonomy-level}) but also reflects a data limitation: only 16 real images were available, insufficient for multi-class generalisation. A multi-class detector would reduce operator workload but increase misclassification risk; for a safety-critical SAR application, human-in-the-loop verification is the more responsible design. This trade-off should be revisited with a larger real-world dataset.

\textbf{Was simulation-first the right strategy?}
Simulation-first consumed approximately 10 of 16 available weeks before hardware was powered on. This produced a mature, well-tested codebase but compressed all hardware integration into a single bench day---creating the exact time pressure the progressive testing framework was designed to avoid. In a second iteration, hardware should be powered on by week~3 at the latest, with simulation and hardware development proceeding in parallel. The simulation infrastructure was valuable, but the 10-week delay to hardware was excessive for a fixed-deadline project.

% ─────────────────────────────────────────────────────────────────────
\subsection{Mission-Level Performance Estimates}\label{sec:mission-metrics}

SAR effectiveness is ultimately measured at the mission level, not subsystem metrics. Table~\ref{tab:mission-metrics} presents estimated mission performance derived from simulation and video proxy data.

\begin{table}[ht]
\centering\small
\caption{Estimated mission-level performance metrics (from SITL simulation and DJI video analysis). All values are estimates; no outdoor mission has been executed.}\label{tab:mission-metrics}
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Metric} & \textbf{Estimated Value} & \textbf{Basis} \\
\midrule
Survey area & \SI{0.04}{\kilo\metre\squared} & KML polygon \\
Coverage rate & ${\sim}$\SI{3200}{\metre\squared\per\minute} & 8\,m/s $\times$ 32\,m swath $\times$ 75\% efficiency \\
Total search time (single pass) & ${\sim}$\SI{12}{\minute} & 0.04\,km$^2$ / 3200\,m$^2$/min \\
Single-pass detection probability & ${\sim}$87\% & DJI video replay (target $>$ 20$\times$20\,px) \\
Two-pass detection probability & ${\sim}$98\% & $1 - (1-0.87)^2$, assuming independence \\
GPS target localisation (after centering) & $<$\SI{2}{\metre} CEP$_{50}$ & Multi-frame averaging during hover \\
Landing offset accuracy & Unknown & Not flight-tested \\
Battery endurance (est.) & ${\sim}$\SI{15}{\minute} & EDU-450 datasheet, conservative \\
\bottomrule
\end{tabular}
\end{table}

The estimated 12-minute single-pass search time fits within the ${\sim}$15-minute battery endurance with a 3-minute margin for centering, descent, and landing. A two-pass strategy would require a battery swap. The 87\% single-pass detection probability is competitive with published systems (Table~\ref{tab:sar-comparison}) but has not been validated outdoors; the centering hover is expected to improve localisation accuracy substantially by eliminating motion-induced errors (P10).

\subsection{Prioritised Future Work}\label{sec:future-priorities}

The nine delta items and design-choice analysis are consolidated into a prioritised action list, ranked by impact on mission readiness per unit of effort.

\begin{enumerate}[nosep]
    \item \textbf{Execute progressive flight tests} (D1, D7) --- Highest impact. All code is flight-ready; the bottleneck is scheduling and weather. One calm day would validate Tiers~4--5 and close R05, R06, R07.
    \item \textbf{Collect held-out test dataset} (D5) --- Record 50+ labelled frames from a real flight and evaluate mAP on a zero-synthetic test split. Required to replace the inflated 0.995 with a credible figure.
    \item \textbf{Deploy NCNN backend} (D2, D9) --- Already benchmarked at 9\,FPS. Requires validation of detection accuracy parity on Pi, then a one-line config change. Estimated effort: 2 hours.
    \item \textbf{Implement GPS lag compensation} (D3) --- Single-line offset ($\Delta\mathbf{p} = \mathbf{v} \cdot \Delta t$) in the heading direction. Tuneable from config. Estimated effort: 1 hour.
    \item \textbf{Run precision--recall threshold sweep} (D4) --- Replay DJI video at thresholds 0.1--0.9 in 0.05 steps, plot PR curve. Estimated effort: 3 hours.
    \item \textbf{Integrate payload servo} (D8) --- Add \texttt{DEPLOY} state with MAV\_CMD\_DO\_SET\_SERVO. Requires wiring and bench test. Estimated effort: 4 hours.
    \item \textbf{Retrain multi-class detector} (D6) --- Requires 100+ labelled images per class from real flights. Dependent on item~1. Estimated effort: 1--2 weeks including data collection.
\end{enumerate}

Items 3--5 are implementable without any flight time and would strengthen the quantitative evidence base immediately. Items 1--2 require airfield access but are the highest-impact improvements for validating the system's real-world capability.

% ─────────────────────────────────────────────────────────────────────
\subsection{Testing Framework Summary}\label{sec:test-framework-summary}

The five-tier progressive testing framework (Table~\ref{tab:five-tier-summary}) advances from pure software to full autonomy, with each tier gating the next. This structure caught 12 defects at low-cost tiers (Appendix~\ref{app:bug-cost}) and is the primary reason no architectural faults remained when hardware was assembled.

\begin{table}[ht]
\centering\small
\caption{Five-tier progressive testing framework (condensed from Appendix~\ref{sec:five-tier}).}\label{tab:five-tier-summary}
\begin{tabular}{@{}clp{7.5cm}@{}}
\toprule
\textbf{Tier} & \textbf{Environment} & \textbf{What It Verifies} \\
\midrule
1 & SITL simulation & State machine logic, path planning, geofence, detection pipeline---zero hardware risk. \\
2 & Docker Pi test & TFLite loads on ARM, dependency compatibility, headless operation. \\
3 & Bench (Pi + Cube, no props) & MAVLink ACKs, camera frames, GPS fix, full sensing chain. \\
4 & Passive flight (pilot on RC) & Real-world detection range, FOV/GPS calibration---Pi issues zero commands. \\
5 & Autonomous flight & End-to-end mission: search, detect, centre, verify, land. \\
\bottomrule
\end{tabular}
\end{table}

Tiers~1--3 were fully exercised; Tier~4 was partially achieved through DJI video replay as a proxy after weather cancellation. Tier~5 remains outstanding. Of the 12 defects (Appendix~\ref{app:bug-cost}), 5 were caught at Tier~1 (simulation, \SI{1.5}{\hour} total fix time), 6 at Tier~2 (Pi bench, \SI{4.7}{\hour}), and 1 at Tier~3 proxy (video analysis, mitigation pending). Two intermediate tiers unique to this project---Docker environment testing (Tier~2) and passive zero-command flight (Tier~4)---are absent from most university drone projects yet caught six of the twelve defects. Appendix~E (\secref{sec:five-tier}) details the full five-tier methodology and per-tier script inventory.

\paragraph{Closing Assessment.}
The system meets its core technical objectives at the subsystem level: \SI{4.8}{FPS} onboard inference on a \pounds75 single-board computer, 87\% single-pass detection rate from flight-altitude video, CEP$_{50}{=}\SI{2.3}{\metre}$ target localisation, and a modular architecture that survived three model retrains and two backend swaps without changing a single line of orchestrator code. Progressive testing caught and resolved 12 integration defects in ${\sim}\SI{6}{\hour}$ of bench time---seven of which would have caused mission-critical failures in flight, including two that would have driven the drone \emph{into} the no-fly zone. These results compare favourably with published SAR systems on hardware costing 3--5$\times$ more (Table~\ref{tab:sar-comparison}).

However, the system remains \emph{incompletely validated}. No outdoor flight was conducted: Tiers~4--5 were never executed, and R05, R06, and R07 are verified only in simulation. The reported mAP$_{50}{=}0.995$ is an upper bound inflated by train/validation overlap; video replay yields ${\sim}$0.87. The GPS timing lag, while characterised, is uncompensated. These are validation gaps, not architectural deficiencies---a single calm flight day would substantially close them.

Of the nine delta items, three are parameter-tuning gaps (D2, D3, D4) resolvable without code changes in under 6 hours combined, two require implementation (D6, D8), and four require flight time and data (D1, D5, D7, D9). The codebase is flight-ready; the bottleneck is scheduling and weather. Completing the remaining tiers requires one calm day at the airfield.

The project demonstrates that the cost and complexity barriers to autonomous SAR are lower than commonly assumed. A Raspberry Pi running a 3.2\,MB neural network can detect a casualty from \SI{35}{\metre} at nearly 5 frames per second---not as a laboratory demonstration, but as deployable, flight-ready software verified through 127 unit tests, 74 integration scripts, and over 100 hours of simulation. The gap between this system and operational deployment is not algorithmic but logistical: more flight time, more real-world training data, and the accumulated confidence that only comes from hours in the air. Appendix~\ref{app:lessons-learned} details the lessons learned and specific improvements for a second iteration.

