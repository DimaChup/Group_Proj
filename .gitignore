__pycache__/
*.pyc
venv/
test_env/
pienv/
project_data/
runs/
*.pt
nul
.claude/
.env
*.env

# Runtime artifacts
flight_log.csv
logs/
passive_flight_log.csv
detection_log.csv
snapshot_*.jpg
detections/
flight_frames/
*.tlog
*.tlog.raw
mav*.parm

# Archive (old/junk files, not deleted)
_archive/

# Experimental model files
Edward/

# Image dumps (screenshots with UUID names)
GP2/
GroupPJ/
follow_mode/

# Dashboard
dashboard/node_modules/
dashboard/dist/

# Large video files (RealVideo — keep .SRT, .csv, .jpg, .png)
RealVideo/*.mp4
RealVideo/*.MP4

# Large/regeneratable files
dataset_v2.zip
dataset_v2/images/
dataset_v2/labels/
dataset_v2/preview_50m/
training/dataset_v2.zip
training/dataset_v2/images/
training/dataset_v2/labels/
calibration_image_sample_data*.npy
*.onnx

# Misc
tests/things to do .txt

# Pi data directory IS tracked (pushed from Pi, pulled to laptop)
# pi_data/ — NOT ignored, always synced via git
!pi_data/
# But large video/photo files are too big for git
pi_data/captured_video/*.avi
pi_data/captured_video/*.mp4
pi_data/captured_video/*.h264
pi_data/captured_photos/*.jpg